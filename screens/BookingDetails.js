import { View, Text } from "react-native";


export const BookingDetails = (props) => {
  const { navigation, route } = props;
  const { params } = route;

  return (
    <View>
      <Text>a</Text>
    </View>
  )
}